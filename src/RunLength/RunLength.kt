package RunLength

/* Using the Java language, have the function RunLength(str) take the str parameter
being passed and return a compressed version of the string using the Run-length encoding algorithm...

Sample Test Cases
Input:"aabbcde"
Output:"2a2b1c1d1e"

Input:"wwwbbbw"
Output:"3w3b1w"
*/

//Not complete!
fun runLength(str: String): String {
    var compressedString = ""
    var charArray = str.toCharArray()
    for ((i, element) in charArray.withIndex()) {

    }

    return compressedString
}